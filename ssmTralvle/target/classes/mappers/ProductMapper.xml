<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tbs.mapper.ProductMapper">
    <!--查询所有用户信息-->
    <select id="findAll" resultType="cn.tbs.pojo.Product">
        select * from product
    </select>

    <insert id="save">
        insert into product values (null,#{productNum},#{productName},#{cityName},#{departureTime},#{productPrice},#{productDesc},#{productStatus})
    </insert>

    <select id="findById" resultType="Product">
        select * from product where id = #{id}
    </select>

    <update id="edit">
        UPDATE `product` SET `id`=#{id}, `productNum`=#{productNum}, `productName`=#{productName}, `cityName`=#{cityName}, `departureTime`=#{departureTime}, `productPrice`=#{productPrice}, `productDesc`=#{productDesc}, `productStatus`=#{productStatus} where id =#{id} ;
    </update>

    <delete id="deleteById">
        delete from product where id = #{id}
    </delete>
</mapper>