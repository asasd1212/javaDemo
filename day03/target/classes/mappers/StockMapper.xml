<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tbs.mapper.StockMapper">
    <!--查询所有用户信息-->
    <select id="findAllStock" resultType="cn.tbs.pojo.Product">
        select *
        from stock
    </select>

    <select id="getStockById" resultType="User">
        select * from stock where id = #{id}
    </select>
</mapper>